---
'@typecad/hal': patch
---

Phase 4 (docs): README rewritten onto the thin HAL — Zephyr-config example, board table without the removed Arduino entries, thin-class quickstart (`new GPIO(LED, GPIO.OUTPUT)` + `Time.sleep`), I2C target construction replacing begin/init-state tracking, ownership/take-release notes, create/install examples, and CLI flag table rows. Only two Arduino mentions remain, both intentional: the removal banner and the installed-package compatibility note.
