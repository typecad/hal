description: Common fields for LCD controllers driving a panel.
include: display-controller.yaml
properties:
  pixel-format:
    type: int
    required: true
    description: Initial pixel format (dt-bindings/display/panel.h).
