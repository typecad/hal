<script lang="ts">
  // app.ui — Stage 2 mono rig test (SSD1309 128x64, self-driving).
  //
  // Everything on screen animates from BINDINGS only (they re-evaluate every
  // ui_tick), so the panel shows motion with no wiring beyond power + I2C:
  //
  //   ┌────────────────────┐  What to verify visually:
  //   │ SSD1309 · MONO │  1. 12px text — the resolution floor for this face
  //   │ t=12.5s            │  2. Uptime counts ~2x/s → full-frame redraw
  //   │ ▓▓▓▓▓▓░░░░░░  47%  │  3. Bar sweeps 0→100% every 5s
  //   │ ██ INVERT ██       │  4. Button face inverts every second
  //   └────────────────────┘
  //
  // If the display shows transposed snow instead of this layout, the vtiled
  // MONO01 packing regressed. If nothing shows: wiring/address (scope the
  // build's TC_DISPLAY lines on the console).

  import { ui } from '@typecad/ui';
  import { Time } from '@typecad/hal';

  ui.mount(screen);

  // Seconds since boot, one decimal.
  ui.bind(screen.uptime, 'text', () => `t=${Math.floor(Time.now() / 100) / 10}s`);
  // 1 Hz square wave drives the button's :pressed value → face inversion.
  ui.bind(screen.invert, 'value', () => Math.floor(Time.now() / 1000) % 2);
</script>

<style>
  @font-face {
    font-family: "RigMono";
    src: url("../../../packages/ui/assets/fonts/dejavu/DejaVuSans.ttf");
    font-weight: 400;
    font-style: normal;
  }

  :root {
    background: #000000;
    color: #ffffff;
    flex-direction: column;
  }

  #titlebar {
    background: #ffffff;
    padding: 1px 3px;
    height: 22px;
  }
  #title {
    color: #000000;
    font-family: "RigMono";
    font-size: 20px;
    font-weight: 400;
  }

  #row2 {
    padding: 0 3px;
    height: 21px;
  }
  #uptime {
    font-family: "RigMono";
    font-size: 20px;
  }

  #row4 {
    display: flex;
    flex-direction: row;
    justify-content: center;
    padding: 0 3px;
    height: 21px;
  }
  #invert {
    font-family: "RigMono";
    font-size: 20px;
    width: 96px;
    min-height: 0;
    height: 19px;
    padding: 0 6px;
    border: 1px solid #ffffff;
    color: #ffffff;
  }
  #invert:pressed {
    background: #ffffff;
    color: #000000;
  }

</style>

<screen>
  <view id="titlebar">
    <text id="title">SSD MONO</text>
  </view>
  <view id="row2">
    <text id="uptime">t=0s</text>
  </view>
  <view id="row4">
    <button id="invert">INVERT</button>
  </view>
</screen>
