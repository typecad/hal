{
  "tasks": [
    {
      "label": "typecad-hal: build + flash (debug)",
      "type": "shell",
      "command": "npx typecad-hal build --compile --upload --debug",
      "options": {
        "cwd": "${workspaceFolder}/src/out"
      },
      "group": {
        "kind": "build",
        "isDefault": false
      },
      "problemMatcher": []
    }
  ]
}
