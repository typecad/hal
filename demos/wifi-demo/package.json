{
  "name": "demo-wifi",
  "version": "1.0.0",
  "private": true,
  "description": "HAL WiFi + HTTP client demos targeting Zephyr",
  "type": "module",
  "scripts": {
    "build": "typecad-hal build",
    "compile": "typecad-hal build --compile",
    "upload": "typecad-hal build --compile --upload --monitor",
    "lint": "eslint src/"
  },
  "dependencies": {
    "@typecad/hal": "*"
  },
  "devDependencies": {
    "eslint": "^10.4.1",
    "@typescript-eslint/parser": "^8.61.0",
    "@typescript-eslint/eslint-plugin": "^8.61.0"
  }
}
