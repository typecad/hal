{
  "name": "demo-wifi",
  "version": "1.0.0",
  "private": true,
  "description": "HAL WiFi + HTTP client demos transpiled to the Arduino-ESP32 core (WiFi / HTTPClient)",
  "type": "module",
  "scripts": {
    "build": "cuttlefish build",
    "compile": "cuttlefish build --compile",
    "upload": "cuttlefish build --compile --upload --monitor",
    "lint": "eslint src/"
  },
  "dependencies": {
    "@typecad/framework-arduino": "*",
    "@typecad/board-esp32-devkit": "*",
    "@typecad/mcu-esp32": "*",
    "@typecad/hal": "*"
  },
  "devDependencies": {
    "eslint": "^10.4.1",
    "@typescript-eslint/parser": "^8.61.0",
    "@typescript-eslint/eslint-plugin": "^8.61.0"
  }
}
