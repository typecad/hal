{
  "name": "demo-contract-board",
  "version": "1.0.0",
  "private": true,
  "description": "Contract demo: a custom STM32F411 PCB described by a typecad.net contract, programmed with no board package \u2014 cuttlefish generates the Zephyr board from the silicon",
  "type": "module",
  "scripts": {
    "build": "cuttlefish build",
    "compile": "cuttlefish build --compile",
    "upload": "cuttlefish build --compile --upload",
    "lint": "eslint src/"
  },
  "dependencies": {
    "@typecad/framework-zephyr": "*"
  },
  "devDependencies": {
    "eslint": "^10.4.1",
    "@typescript-eslint/parser": "^8.61.0",
    "@typescript-eslint/eslint-plugin": "^8.61.0"
  }
}
