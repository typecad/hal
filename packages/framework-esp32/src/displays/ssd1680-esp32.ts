import type { DisplayAdapterGenerator } from "@typecad/cuttlefish/api/shared";

// Stub — real implementation lands in Task 16.
export const esp32Ssd1680Adapter: DisplayAdapterGenerator = (_display) => {
  throw new Error("esp32Ssd1680Adapter: not yet implemented (Task 16)");
};
